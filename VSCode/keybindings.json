// Place your key bindings in this file to overwrite the defaults
[
    {
        "key": "ctrl+alt+f",
        "command": "runInTerminal.run",
        "args": {
            "cmd": "node_modules/.bin/eslint --fix ${relativeFile}",
            "match": ".*"
        }
    }
]